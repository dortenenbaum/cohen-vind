# מסירה למתכנת — פורמט היעד

הלקוח ימסור את האתר למתכנת שיחבר אותו לוורדפרס. הערכה הזו נכתבה אחרי
קריאה של ערכת מסירה קיימת של אותו בית תוכנה (Binternet), כדי שנדע לאן
אנחנו הולכים ולא נצטרך לפרק ולהרכיב בסוף.

## איך הערכה שלהם בנויה

**קובץ HTML סטטי לכל תבנית.** `template-home.html`, `template-project.html`,
`template-projects.html`, `template-about.html`, `template-contact.html`,
`template-press.html`. אין PHP במסירה — המתכנת ממיר כל קובץ לתבנית
וורדפרס בעצמו, והניתוב הוא של וורדפרס.

**BEM, כשהבלוק הוא שם הסקשן.** `section_project-carousel__heading`,
`header__logo-wrap`. לצד זה `js-` כווים לוגיים נפרדים מהעיצוב:
`js-header`, `js-section_project-carousel` — כך שאפשר לשנות מחלקת עיצוב
בלי לשבור התנהגות.

**SCSS: פרשיה אחת לכל סקשן** תחת `__src/scss/sections/`, מיובאות
ב-`main.scss`. מתחתן `base/` — normalize, fonts, colors, config, grid,
typography, general, extends — ו-`tools/` עם המיקסינים. נקודות שבירה
דרך `@include mq($xl)`.

**Gulp:** SCSS ל-`assets/css/main.css`, autoprefixer, איחוד מדיה-קוורי,
browser-sync. `package.json` + `gulpfile.js` בשורש.

**JS: קובץ לכל סקשן** תחת `assets/js/sections/`, כל אחד IIFE שיוצא מיד
אם הסקשן לא בעמוד. ספריות ב-`assets/js/vendor/`: jQuery, Swiper, GSAP
(עם ScrollTrigger, SplitText, TextPlugin), lity, mixitup.

**גופנים** מוגשים מקומית: `assets/fonts/` + `base/_fonts.scss` עם
`@font-face` לכל משקל, בפורמטים woff2/woff/ttf/eot.

**תוכן חוזר** הוא מארקאפ חוזר עם `data-project-for="1"` וכדומה — המתכנת
ממפה אותו ל-repeater או ל-CPT בצד הוורדפרס.

## מה אצלנו שונה, ומה זה אומר

| אצלנו | אצלם | המשמעות |
|---|---|---|
| קובץ אחד עם ראוטר hash | קובץ לכל תבנית | **הפיצול לקבצים הוא העבודה העיקרית.** בוורדפרס הניתוב הוא של וורדפרס; הראוטר שלנו צריך לרדת. |
| התוכן במערכים ב-JS (`P`, `CAROUSEL`, `TEAM_ROLES`, `PRINCIPLES`, `CAREERS`) והמארקאפ נבנה מהם | התוכן במארקאפ | **כל מערך תוכן צריך להיות מארקאפ בתבנית**, וה-JS צריך לקרוא מה-DOM במקום לייצר אותו. זה החלק הכבד. |
| CSS אחד עם טוקנים ב-`:root` | פרשיות SCSS + `base/_colors` `_config` | הטוקנים שלנו עוברים כמעט אחד־לאחד. |
| מחלקות קצרות (`.crsl`, `.cslide`, `.lede-in`) | BEM לפי שם סקשן | שינוי שמות מכני, אפשר לעשות בסוף. |
| אפס ספריות | GSAP, Swiper, jQuery | ההתנהגויות שלנו כתובות ביד. זה יתרון — אבל צריך לוודא שהן עוברות כמו שהן ולא "מוחלפות" בספרייה. |

## מה שווה לעשות כבר עכשיו

- **לא לפצל לקבצים בינתיים.** עבודה על קובץ אחד מהירה בהרבה, והפיצול הוא
  פעולה מכנית שנעשית פעם אחת בסוף.
- **להחזיק כל תוכן במערך אחד מוצהר בראש הקובץ** ולא לפזר מחרוזות בתוך
  הפונקציות. ככה ההמרה למארקאפ היא העתקה, לא ארכיאולוגיה.
- **לשמור על גבול סקשן ברור** — כל סקשן פונקציית תבנית אחת + בלוק CSS
  אחד, כדי שהחיתוך לפרשיות יהיה לפי קווים קיימים.
- **לא להסתמך על הראוטר** להתנהגות שאינה ניווט.

## מה יידרש בהמרה עצמה

1. פיצול ל-10 תבניות: בית, פרויקטים, פרויקט, החזון שלנו, צוות, קורות חיים,
   אודות, קריירה, משרה, מאמר.
2. הוצאת `P`, `CAROUSEL`, `TEAM_ROLES`, `PRINCIPLES`, `CAREERS`, `TRACK`
   למארקאפ, והסבת ה-JS לקריאה מה-DOM.
3. פירוק ה-CSS לפרשיות סקשן + `base/`.
4. הוספת `js-` כווים לאלמנטים שה-JS תופס היום לפי מחלקת עיצוב.
5. הגופן: ראו "הפונט במסירה" ב-README.
